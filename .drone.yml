---
kind: pipeline
type: docker
name: default

steps:
- name: Build ModularKit
  image: ubuntu
  commands:
  - apt update && apt install git apache-maven openjdk-8-jdk make -y
  - make clean build-nightly

trigger:
  branch:
  - main